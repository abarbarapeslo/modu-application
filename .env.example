# Dev — Postgres local (docker, pgAdmin, instalado na máquina)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/modu?schema=public"

# MVP / staging / prod — Supabase (Project Settings → Database → Connection string → URI)
# Use pooling na Vercel/serverless: porta 6543 + ?pgbouncer=true
# DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
